# XCP-Based AI Validation Interface Mock

Open `index.html` locally.

This mock demonstrates a public UI concept for signal replay, AI output comparison, validation gate status, and event logging. The original internal project context involved ADAS AI validation workflows, but this public version uses synthetic CSV data and a mock transport layer.
